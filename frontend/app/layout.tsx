export const metadata = { title: 'DClaw Waste', description: 'Waste reduction AI' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
